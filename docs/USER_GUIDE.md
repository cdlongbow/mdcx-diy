# 用户使用手册

本手册介绍 MDCX 的基本使用方法和常见操作。

## 目录

1. [快速开始](#快速开始)
2. [主要功能](#主要功能)
3. [基本操作](#基本操作)
4. [演员数据库管理](#演员数据库管理)
5. [TMDB 数据刮削](#tmdb-数据刮削)
6. [故障排除](#故障排除)

## 快速开始

### 首次启动

1. 运行程序后，你会看到主界面
2. 程序会自动检测并加载演员数据库（`userdata/actor_database.xlsx`）
3. 首次使用时，建议先检查配置设置

### 界面介绍

主界面包含以下主要区域：
- **菜单栏**：文件、编辑、工具、帮助
- **工具栏**：常用操作按钮
- **数据面板**：显示演员信息和搜索结果
- **状态栏**：显示当前操作状态和提示信息

## 主要功能

### 1. 演员数据库管理

演员数据库为 Excel 文件（`userdata/actor_database.xlsx`），基本操作在 Excel 中完成：

- 使用 Excel 打开数据库文件
- 查看和编辑演员信息（日文名、中文名、繁体名、别名、信息链接、TMDB ID 等）
- 使用 Excel 的筛选和搜索功能查找演员
- 在 Excel 中直接修改数据后保存

### 2. TMDB 数据刮削

- 刮削时从 TMDB 获取演员 ID 和多语言信息
- 自动补全缺失的演员 TMDB ID
- 自动匹配和补全数据
- 限速控制避免 API 超限

### 3. 数据格式化

- 自动格式化数据库
- 规范信息链接
- 清理重复数据
- 优化存储结构

## 基本操作

### 搜索演员

演员数据库为 Excel 文件，搜索在 Excel 中完成：

1. 打开 `userdata/actor_database.xlsx`
2. 使用 Excel 的筛选功能（数据 > 筛选）
3. 在筛选下拉菜单中选择搜索范围（日文名、中文名、别名等列）
4. 输入关键词快速定位演员

也可使用 Ctrl+F 在 Excel 中全局搜索。

### 编辑演员信息

演员数据库为 Excel 文件，直接在 Excel 中编辑：

1. 打开 `userdata/actor_database.xlsx`
2. 找到需要修改的演员行
3. 直接修改日文名、中文名、繁体名、别名、信息链接或 TMDB ID
4. 保存 Excel 文件

程序启动时会自动加载最新数据。

### 备份数据

演员数据库为 Excel 文件，直接复制备份：

1. 关闭程序（确保文件未被占用）
2. 复制 `userdata/actor_database.xlsx` 到安全位置
3. 恢复时直接替换原文件即可

## 演员数据库管理

### 数据库结构

演员数据库包含以下字段：

- **ID**：唯一标识符
- **日文名**：演员日文原名
- **中文名**：中文姓名
- **繁体名**：繁体中文姓名
- **别名**：其他名称
- **信息链接**：相关网站链接（LibreDMM、JavDB、TMDB 链接等）
- **TMDB ID**：TMDB 数据库 ID

### 数据维护

#### 格式化数据库

定期格式化数据库以保持数据一致性：

1. 工具 > 格式化数据库
2. 程序会自动：
   - 清理无效数据
   - 规范信息链接
   - 统一格式
3. 查看格式化报告

#### 备份数据

建议定期手动复制 `userdata/actor_database.xlsx` 到安全位置。

## TMDB 数据刮削

### 刮削时自动补全

1. 在配置中填写 TMDB API Key
2. 开始刮削影片
3. 程序会自动：
   - 检查演员数据库中的 TMDB ID
   - 优先使用本地演员数据库中的日文名、中文名、繁体名和别名反查缓存
   - 对仍然缺失的演员调用 TMDB API
   - 将匹配结果写回 `userdata/actor_database.xlsx`
4. 查看生成的 NFO 和演员数据库

### TMDB 配置

TMDB API 配置：
- 访问 [TMDB API](https://www.themoviedb.org/settings/api) 获取 API Key
- 在设置中输入 API Key
- 保存配置

**注意**：TMDB 查询内置了限流和并发控制，默认约 3.5 req/s、3 路并发。

## 高级功能

### API 数据补全

演员信息可以通过多个数据源补全：

1. **TMDB API**：电影数据库信息
2. **Wikidata**：维基百科数据
3. **Gfriends**：演员头像和写真
4. **graphis.ne.jp**：写真集信息

详细说明请参阅 [TMDB 数据流文档](CODE_WIKI.md#tmdb-数据流)。

### Emby/Jellyfin 集成

支持与 Emby 和 Jellyfin 媒体服务器集成：

- 自动补全演员信息
- 获取演员简介
- 更新演员头像

配置方法请参阅 [Emby 集成文档](CODE_WIKI.md#emby-集成)。

## 故障排除

### 常见问题

#### 1. TMDB API 限流错误

**症状**：频繁出现 429 Too Many Requests 错误

**解决方案**：
- 降低并发数（建议 3）
- 启用令牌桶限流（默认 3.5 req/s）
- 等待一段时间后重试

#### 2. 数据库加载失败

**症状**：程序启动时无法加载数据库

**解决方案**：
- 检查文件路径是否正确
- 确认文件未被其他程序占用
- 尝试恢复备份文件

#### 3. 搜索无结果

**症状**：搜索演员时没有结果

**解决方案**：
- 检查搜索词是否正确
- 尝试使用别名或不同语言的名称
- 检查演员名和别名，必要时手动修正演员数据库后重新刮削

#### 4. GUI 显示异常（Linux）

**症状**：界面显示不正常或崩溃

**解决方案**：
```bash
# 安装缺失的系统库
sudo apt install libxcb-xinerama0 libxcb-cursor0

# 设置环境变量
export QT_QPA_PLATFORM=xcb
```

### 获取更多帮助

- 查看 [FAQ](FAQ.md) 获取更多常见问题解答
- 访问 [GitHub Issues](https://github.com/cdlongbow/mdcx/issues) 提交问题
- 阅读 [开发文档](DEVELOPMENT.md) 了解技术细节

## 键盘快捷键

| 快捷键 | 功能 |
|--------|------|
| Ctrl+F | 搜索 |
| Ctrl+N | 新建 |
| Ctrl+S | 保存 |
| Ctrl+E | 编辑 |
| Ctrl+D | 删除 |
| Ctrl+R | 刷新 |
| F1 | 帮助 |

## 下一步

- 查看 [配置说明](configuration.md) 了解详细配置
- 阅读 [FAQ](FAQ.md) 解决常见问题
- 了解 [开发文档](DEVELOPMENT.md) 参与贡献
